// PROJECT IMPORTS
import LoginView from "@/views/auth/login/LoginView";

// ==============================|| Login ||============================== //

export const metadata = {
    title: "Login",
    description: "Login",
};

const Login = () => {
    return <LoginView />;
};

export default Login;
